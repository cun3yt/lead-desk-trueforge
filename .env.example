# Local secrets — gitignored. Fill in values; never commit.

# Anthropic key, registered into TrueForge as the model provider by trueforge/setup.sh
ANTHROPIC_API_KEY=

# Ambiguous agent API key (ak_…), used as the MCP connector header and by trueforge/reset.sh
AMBI_API_TOKEN=
AMBI_API_URL=https://app.ambiguous.ai
